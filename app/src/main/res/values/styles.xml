<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Action Bar -->

    <style name="Widget.Crafty.Light.ActionBar" parent="android:Widget.Holo.Light.ActionBar">
        <item name="android:background">@color/theme_default_primary</item>
        <item name="android:displayOptions">showTitle</item>
        <item name="android:titleTextStyle">@style/TextAppearance.Crafty.Widget.ActionBar.Title</item>
    </style>

    <style name="TextAppearance.Crafty.Widget.ActionBar.Title"
           parent="android:TextAppearance.Holo.Widget.ActionBar.Title">
        <item name="android:textColor">?actionBarIconColor</item>
    </style>

    <!-- Drop down lists -->

    <style name="Widget.Crafty.ListView.DropDown" parent="android:Widget.Holo.ListView.DropDown">
        <item name="android:divider">@android:color/transparent</item>
        <item name="android:dividerHeight">0dp</item>
    </style>
</resources>
